# システムの未決事項

運用ルールと構成は `AGENTS.md` が正本。ここには「まだやっていないこと」だけを置く。
変更の履歴は git log を参照。

## 保留中

- **携帯からの capture 経路(同期)**:思いついたことの取りこぼしが現状いちばん大きい入口の穴。
  本人判断で保留中。それまでは「思いついたら daily に書く」に一元化する。

## 解決済み

- ~~小さいアイデアの再浮上~~ → `$resurface-ideas` として実装(2026-07-13)。
- ~~skill 定義がツール依存~~ → 正本を `_meta/` に集約し、各ツールは発見用スタブに(2026-08-10)。
- ~~空のデイリーが残り続ける~~ → `$tag-dailies` が検出して `_trash/` へ移動(2026-08-10)。削除ではないので承認は要らない。
- ~~目標を書いても振り返らない~~ → Monthly Goals と `$monthly-review`(2026-08-10)。
