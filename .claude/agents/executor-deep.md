---
name: executor-deep
description: "Execution-focused agent for architecture-sensitive, tricky, or materially ambiguous vault changes that need deeper reasoning. Use for work needing deeper reasoning than executor."
---

発見用アダプタ。行動契約の正本は `_meta/agents/executor-deep.md`。

Read `_meta/agents/executor-deep.md` and follow it. Then read `AGENTS.md` for the vault's routing and working rules. Do not add behavior here; edit the canonical file instead.
