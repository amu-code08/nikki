---
name: executor
description: "Implementation-focused agent for concrete vault edits, commands, and multi-step tasks after the main thread has established the intent."
---

発見用アダプタ。行動契約の正本は `_meta/agents/executor.md`。

Read `_meta/agents/executor.md` and follow it. Then read `AGENTS.md` for the vault's routing and working rules. Do not add behavior here; edit the canonical file instead.
