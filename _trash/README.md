# _trash

`$tag-dailies` が本文の空なデイリーをここへ移す。削除ではなく移動なので取り消せる。

- ファイル名は元のまま(`YYYY-MM-DD.md`)。
- 分析対象外。`$extract` `$open-todos` `$resurface-ideas` は読まない。
- **ここから消すのは人間だけ。**エージェントは移動しかしない。
- 時々中を見て、本当に空なら消す。空判定の誤りに気づく場所でもある。
