# _trash

`$tag-dailies` が散文の無いデイリーをここへ移す。本文が空の日と、ToDo リストしか書かなかった日の両方が対象。削除ではなく移動なので取り消せる。

- ファイル名は元のまま(`YYYY-MM-DD.md`)。
- 分析対象外。抽出・todo 収集・`$resurface-ideas` のいずれも読まない。
- **ここから消すのは人間だけ。**エージェントは移動しかしない。
- 時々中を見て、本当に空なら消す。空判定の誤りに気づく場所でもある。
